# Losawa Admin Panel

Админ-панель для управления интернет-магазином Losawa.

## 🚀 Быстрый старт

### 1. Создание администратора

Создайте первого администратора:

```bash
npx tsx scripts/create-admin.ts
```

Введите email и пароль для администратора.

### 2. Доступ к админ-панели

1. Откройте `http://localhost:3000/admin/login`
2. Введите email и пароль администратора
3. После входа вы будете перенаправлены на `/admin/dashboard`

## 📋 Функционал

### ✅ Реализовано

#### 🔐 Авторизация
- Логин по email/password
- Сессии через cookies
- Защита всех админ-роутов
- Автоматический редирект на логин при отсутствии авторизации

#### 📦 Управление товарами
- **Список товаров** (`/admin/products`)
  - Таблица со всеми товарами
  - Поиск по названию
  - Фильтры по категории и коллекции
  - Переключение статуса (активен/скрыт)
  - Редактирование и удаление товаров
  
- **Создание товара** (`/admin/products/new`)
  - Все поля товара
  - Загрузка изображения
  - Выбор категории и коллекции
  - Установка статуса
  
- **Редактирование товара** (`/admin/products/[id]`)
  - Редактирование всех полей
  - Замена изображения
  - Изменение статуса

#### 🗂 Управление категориями
- **Список категорий** (`/admin/categories`)
  - Таблица всех категорий
  - Количество товаров в каждой категории
  - Создание/редактирование/удаление
  - Автоматическая генерация slug из названия
  - Защита от удаления категорий с товарами

#### 🧵 Управление коллекциями
- **Список коллекций** (`/admin/collections`)
  - Карточки коллекций с изображениями
  - Количество товаров в коллекции
  - Создание/редактирование/удаление
  - Все поля: name, slug, description, image, subtitle, quote, fullDescription
  - Загрузка изображений

#### 🛒 Управление заказами
- **Список заказов** (`/admin/orders`)
  - Таблица всех заказов
  - Фильтр по статусу
  - Детальная информация о заказе в боковой панели
  - Изменение статуса заказа
  - Просмотр товаров в заказе
  - Информация о клиенте и адресе доставки

#### 🖼 Загрузка изображений
- Endpoint `/api/admin/upload`
- Автоматическое сохранение в `/public/products/`
- Валидация типов файлов
- Генерация уникальных имен файлов

### 📊 Dashboard
- Статистика по товарам, категориям, коллекциям, заказам
- Количество pending заказов
- Быстрые действия (создание товара, управление категориями/коллекциями)

## 🔒 Безопасность

- Все API endpoints защищены через `requireAdmin()`
- Пароли хешируются с помощью bcrypt
- Сессии хранятся в httpOnly cookies
- Проверка авторизации на каждом запросе

## 📁 Структура

```
app/admin/
├── layout.tsx              # Layout с сайдбаром и навигацией
├── login/page.tsx          # Страница входа
├── dashboard/page.tsx       # Главная страница админки
├── products/
│   ├── page.tsx            # Список товаров
│   ├── new/page.tsx        # Создание товара
│   └── [id]/page.tsx       # Редактирование товара
├── categories/
│   └── page.tsx            # Управление категориями
├── collections/
│   └── page.tsx            # Управление коллекциями
└── orders/
    └── page.tsx            # Управление заказами

app/api/admin/
├── auth/
│   ├── login/route.ts      # Вход
│   ├── logout/route.ts     # Выход
│   └── me/route.ts        # Проверка авторизации
├── products/
│   ├── route.ts            # GET (список), POST (создание)
│   └── [id]/route.ts      # GET, PUT, DELETE
├── categories/
│   ├── route.ts            # GET (список), POST (создание)
│   └── [id]/route.ts      # GET, PUT, DELETE
├── collections/
│   ├── route.ts            # GET (список), POST (создание)
│   └── [id]/route.ts      # GET, PUT, DELETE
├── orders/
│   ├── route.ts            # GET (список)
│   └── [id]/route.ts      # GET, PUT (изменение статуса)
└── upload/route.ts         # Загрузка изображений
```

## 🎨 Дизайн

- Цветовая схема соответствует основному сайту (#3D2B57)
- Адаптивный дизайн для мобильных устройств
- Боковое меню навигации
- Мобильное меню с overlay

## 🔄 Статусы товаров

- **Active** - товар виден на сайте
- **Hidden** - товар скрыт, но не удален (можно восстановить)

## 📝 Статусы заказов

- **pending** - Ожидает обработки
- **processing** - В обработке
- **shipped** - Отправлен
- **delivered** - Доставлен
- **cancelled** - Отменен

## 🛠 Команды

```bash
# Создать администратора
npx tsx scripts/create-admin.ts

# Запустить dev сервер
npm run dev

# Открыть админку
# http://localhost:3000/admin/login
```

## 📝 Примечания

- Изображения сохраняются в `/public/products/`
- При удалении категории проверяется наличие товаров
- При удалении коллекции товары не удаляются (collectionId становится null)
- Все изменения сразу отражаются на сайте (для активных товаров)
