# Losawa - Полное описание функционала

## Обзор проекта

Losawa - это современный адаптивный интернет-магазин ювелирных изделий, построенный на Next.js 14 с использованием TypeScript, Tailwind CSS и Prisma ORM с SQLite базой данных.

---

## 🗄️ Бэкенд (Backend)

### Технологии
- **Next.js 14** - App Router для API routes
- **Prisma ORM 7** - ORM для работы с базой данных
- **SQLite** - База данных (через better-sqlite3)
- **TypeScript** - Типизация

### База данных (Prisma Schema)

#### Модели данных:

1. **Category (Категории)**
   - `id` (String, CUID) - Уникальный идентификатор
   - `slug` (String, Unique) - URL-слаг категории
   - `name` (String) - Название категории
   - `description` (String, Optional) - Описание категории
   - `createdAt`, `updatedAt` - Временные метки
   - Связь: один-ко-многим с Product

2. **Collection (Коллекции)**
   - `id` (String, CUID) - Уникальный идентификатор
   - `slug` (String, Unique) - URL-слаг коллекции
   - `name` (String) - Название коллекции
   - `description` (String) - Краткое описание
   - `image` (String) - Путь к изображению коллекции
   - `subtitle` (String, Optional) - Подзаголовок
   - `quote` (String, Optional) - Цитата для коллекции
   - `fullDescription` (String, Optional) - Полное описание
   - `createdAt`, `updatedAt` - Временные метки
   - Связь: один-ко-многим с Product

3. **Product (Товары)**
   - `id` (Int, Auto-increment) - Уникальный идентификатор
   - `name` (String) - Название товара
   - `price` (Float) - Цена товара
   - `image` (String) - Путь к изображению товара
   - `material` (String) - Материал изделия
   - `description` (String, Optional) - Описание товара
   - `dateAdded` (DateTime) - Дата добавления
   - `createdAt`, `updatedAt` - Временные метки
   - `categoryId` (String, Foreign Key) - Связь с Category
   - `collectionId` (String, Optional, Foreign Key) - Связь с Collection
   - Связи: многие-к-одному с Category и Collection, один-ко-многим с OrderItem

4. **Order (Заказы)**
   - `id` (String, CUID) - Уникальный идентификатор заказа
   - `customerName` (String) - Имя клиента
   - `email` (String) - Email клиента
   - `phone` (String, Optional) - Телефон клиента
   - `address` (String) - Адрес доставки
   - `city` (String) - Город
   - `country` (String, Default: "USA") - Страна
   - `postalCode` (String, Optional) - Почтовый индекс
   - `status` (String, Default: "pending") - Статус заказа (pending, processing, shipped, delivered, cancelled)
   - `total` (Float) - Общая сумма заказа
   - `notes` (String, Optional) - Дополнительные заметки
   - `createdAt`, `updatedAt` - Временные метки
   - Связь: один-ко-многим с OrderItem

5. **OrderItem (Позиции заказа)**
   - `id` (String, CUID) - Уникальный идентификатор
   - `quantity` (Int, Default: 1) - Количество товара
   - `price` (Float) - Цена на момент заказа
   - `createdAt` - Временная метка
   - `orderId` (String, Foreign Key) - Связь с Order
   - `productId` (Int, Foreign Key) - Связь с Product
   - Связи: многие-к-одному с Order и Product

### API Endpoints

#### 1. Products API (`/api/products`)

**GET /api/products**
- Получение всех товаров с опциональными фильтрами
- Query параметры:
  - `category` (string) - Фильтр по категории (slug)
  - `collection` (string) - Фильтр по коллекции (slug)
  - `material` (string) - Фильтр по материалу (частичное совпадение)
  - `minPrice` (number) - Минимальная цена
  - `maxPrice` (number) - Максимальная цена
- Возвращает: массив товаров с вложенными объектами category и collection
- Пример: `/api/products?category=rings&minPrice=5000&maxPrice=10000`

**GET /api/products/[id]**
- Получение одного товара по ID
- Возвращает: объект товара с полной информацией

#### 2. Collections API (`/api/collections`)

**GET /api/collections**
- Получение всех коллекций
- Возвращает: массив коллекций, отсортированных по имени
- Используется для отображения блока "Our Collections"

**GET /api/collections/[slug]**
- Получение одной коллекции по slug
- Возвращает: объект коллекции с массивом связанных товаров
- Используется для страницы коллекции

#### 3. Categories API (`/api/categories`)

**GET /api/categories**
- Получение всех категорий
- Возвращает: массив категорий с количеством товаров в каждой
- Используется для навигации и фильтрации

#### 4. Orders API (`/api/orders`)

**POST /api/orders**
- Создание нового заказа
- Тело запроса:
  ```json
  {
    "customerName": "string",
    "email": "string",
    "phone": "string (optional)",
    "address": "string",
    "city": "string",
    "country": "string (optional)",
    "postalCode": "string (optional)",
    "items": [
      {
        "productId": number,
        "quantity": number
      }
    ],
    "notes": "string (optional)"
  }
  ```
- Автоматически рассчитывает общую сумму заказа
- Создает связанные OrderItem записи
- Возвращает: созданный заказ с полной информацией

**GET /api/orders**
- Получение списка заказов
- Query параметры:
  - `email` (string) - Фильтр по email клиента
- Возвращает: массив заказов с позициями и товарами

#### 5. Debug API (`/api/debug`)

**GET /api/debug**
- Диагностический endpoint для проверки подключения к БД
- Возвращает: информацию о подключении, количестве товаров и коллекций

#### 6. Health API (`/api/health`)

**GET /api/health**
- Проверка статуса сервера
- Возвращает: статус, timestamp, DATABASE_URL

### Утилиты и хелперы

**lib/prisma.ts**
- Singleton паттерн для Prisma Client
- Инициализация с адаптером better-sqlite3
- Глобальное кэширование для предотвращения множественных подключений

**lib/api.ts**
- Типы данных (Product, Collection, Category)
- Функции для работы с API:
  - `getProducts(filters?)` - Получение товаров
  - `getProduct(id)` - Получение одного товара
  - `getCollections()` - Получение коллекций
  - `getCollection(slug)` - Получение коллекции
  - `getCategories()` - Получение категорий
  - `createOrder(orderData)` - Создание заказа

**hooks/useProducts.ts**
- React хук для получения товаров
- Автоматическая фильтрация невалидных данных
- Состояния: loading, error, products

**hooks/useCollections.ts**
- React хук для получения коллекций
- Состояния: loading, error, collections

### Seed скрипты

**scripts/seed-db.ts**
- Заполнение базы данных начальными данными
- Использует прямой доступ к SQLite через better-sqlite3
- Импортирует данные из `data/products.ts` и `data/collections.ts`
- Создает категории, коллекции и товары
- Запуск: `npm run db:seed`

---

## 🎨 Фронтенд (Frontend)

### Технологии
- **Next.js 14** - React framework с App Router
- **React 18** - UI библиотека
- **TypeScript** - Типизация
- **Tailwind CSS** - Utility-first CSS framework
- **Lucide React** - Иконки

### Структура страниц

#### 1. Главная страница (`/`)

**Компоненты:**
- `Header` - Шапка сайта с логотипом, навигацией и корзиной
- `Hero` - Главный баннер с каруселью изображений
- `Collections` - Блок "Our Collections" с карточками коллекций
- `ShopByCategoryFilter` - Блок "Shop by Category" с фильтрами и товарами
- `SplitBanner` - Баннер "Losawa Fine Jewelry" (split-panel)
- `BestsellersBlock` - Блок бестселлеров с сеткой товаров
- `PromoBanner` - Баннер "Discover Timeless Elegance" (split-panel, зеркальный)
- `PerfectGift` - Блок "Perfect Gift"
- `GiftingServices` - Блок услуг подарков
- `Newsletter` - Форма подписки на рассылку
- `Footer` - Подвал сайта с логотипом и ссылками

**Функционал:**
- Загрузка товаров через API (`useProducts` hook)
- Фильтрация товаров по категориям (Rings, Earrings, Necklaces, Bracelets)
- Отображение 6 товаров на категорию с кнопкой "More"
- Добавление товаров в корзину (локальное состояние)
- Переход на страницу коллекции при клике на карточку коллекции
- Адаптивный дизайн для мобильных устройств

#### 2. Страница магазина (`/shop`)

**Функционал:**
- Отображение всех товаров или фильтрация по коллекции (query параметр `?collection=slug`)
- Фильтры:
  - По материалу (Yellow Gold, White Gold, Rose Gold, Silver)
  - По цене (диапазоны: Under $5,000, $5,000-$10,000, и т.д.)
- Сортировка:
  - Featured (по умолчанию)
  - Newest (новые сначала)
  - Price: Low to High
  - Price: High to Low
- Отображение описания коллекции при фильтрации по коллекции
- Адаптивный дизайн с мобильными фильтрами (overlay)
- Breadcrumb навигация
- Счетчик активных фильтров

**Компоненты:**
- Sidebar с фильтрами (десктоп)
- Mobile filters overlay (мобильные устройства)
- Product grid с карточками товаров
- Toolbar с кнопкой фильтров и сортировкой

#### 3. Страница категории (`/category/[slug]`)

**Функционал:**
- Отображение товаров конкретной категории
- Те же фильтры и сортировка, что и на странице `/shop`
- Динамический заголовок страницы на основе категории
- Breadcrumb навигация

**Поддерживаемые категории:**
- `/category/rings` - Кольца
- `/category/earrings` - Серьги
- `/category/necklaces` - Ожерелья и подвески
- `/category/bracelets` - Браслеты

#### 4. Страница товара (`/product/[id]`)

**Функционал:**
- Отображение детальной информации о товаре
- Изображение товара
- Название, цена, материал, описание
- Кнопка добавления в корзину
- Breadcrumb навигация

#### 5. Страница "About Us" (`/about`)

**Функционал:**
- Информация о бренде Losawa
- История создания бренда
- Философия и ценности
- Изображения и текстовые блоки
- Стилизация в соответствии с общим дизайном сайта

### Компоненты

#### Навигационные компоненты

**Header.tsx**
- Логотип (кликабельный, ведет на главную)
- Навигационное меню:
  - Shop
  - Collections
  - About Us
- Иконка корзины с счетчиком товаров
- Мобильное меню (hamburger)
- Sticky header при прокрутке

**Footer.tsx**
- Логотип (белый, инвертированный)
- Колонки ссылок:
  - Quick Links (Shop, Collections, About Us)
  - Customer Service
  - Connect
- Социальные сети
- Copyright информация

#### Блоки контента

**Hero.tsx**
- Карусель изображений с автоматической прокруткой
- Заголовки и описания для каждого слайда
- Кнопки призыва к действию
- Адаптивные изображения

**Collections.tsx**
- Сетка коллекций (2 колонки на мобильных, 4 на десктопе)
- Карточки коллекций с изображениями
- Градиентные overlay на изображениях
- Кнопка "Explore Collection" на каждой карточке
- Кнопка "View All Collections"
- Загрузка данных через API (`useCollections` hook)

**ShopByCategoryFilter.tsx**
- Кнопки фильтров категорий (All, Rings, Earrings, Necklaces, Bracelets)
- Активное состояние кнопок (цвет #3D2B57)
- Сетка товаров (2 колонки на мобильных, 4 на десктопе)
- Карточки товаров с изображениями, названием, ценой
- Кнопка добавления в корзину на каждой карточке
- Блок "More" для перехода на страницу категории
- Отображение 6 товаров на категорию

**BestsellersBlock.tsx**
- Сетка товаров (2x4 на десктопе)
- Специальные текстовые блоки:
  - "Always out of order — hurry up"
  - "Shop all"
- Карточки товаров с кнопками добавления в корзину
- Заголовок "Bestsellers" с ссылкой "See all"

**SplitBanner.tsx**
- Split-panel дизайн (текст слева, изображение справа)
- Градиентный фон на текстовой панели
- Заголовок "Losawa Fine Jewelry"
- Описание и кнопка "Shop Now"
- Адаптивный дизайн

**PromoBanner.tsx**
- Split-panel дизайн (зеркальный: изображение слева, текст справа)
- Заголовок "Discover Timeless Elegance"
- Описание и кнопка "Shop Now"
- Градиентный фон на текстовой панели

**PerfectGift.tsx**
- Блок с информацией о подарках
- Карточки с описанием услуг

**GiftingServices.tsx**
- Блок услуг подарков
- Иконки и описания услуг

**Newsletter.tsx**
- Форма подписки на рассылку
- Поле ввода email
- Кнопка подписки

#### Утилитарные компоненты

**ProductGrid.tsx**
- Переиспользуемый компонент для отображения сетки товаров
- Принимает массив товаров и функцию добавления в корзину

**FeaturedProducts.tsx**
- Компонент для отображения избранных товаров
- Используется в различных блоках сайта

**CategorySlider.tsx**
- Горизонтальный слайдер категорий
- Карточки товаров в слайдере

### Стилизация

**Дизайн-система:**
- Основной цвет: `#3D2B57` (фиолетовый)
- Шрифт: Raleway (Google Fonts)
- Цветовая палитра:
  - Белый фон
  - Серые оттенки для текста
  - Градиенты для баннеров
- Тени: `shadow-sm` для карточек товаров
- Скругления: `rounded-xl`, `rounded-2xl`

**Адаптивность:**
- Mobile-first подход
- Breakpoints:
  - `sm:` - 640px
  - `md:` - 768px
  - `lg:` - 1024px
  - `xl:` - 1280px
- Мобильные версии:
  - 2 колонки для товаров
  - 2 колонки для коллекций
  - Вертикальное меню
  - Overlay фильтры

### Состояние приложения

**Корзина (Cart)**
- Локальное состояние (useState)
- Хранение ID товаров в массиве
- Счетчик товаров в Header
- Функция добавления товаров в корзину

**Загрузка данных**
- React hooks для загрузки данных из API
- Состояния loading и error
- Автоматическая фильтрация невалидных данных
- Кэширование через React hooks

### Маршрутизация

**Next.js App Router:**
- `/` - Главная страница
- `/shop` - Страница магазина
- `/shop?collection=slug` - Фильтрация по коллекции
- `/category/[slug]` - Страница категории
- `/product/[id]` - Страница товара
- `/about` - Страница "About Us"

**Навигация:**
- Client-side navigation через `next/link`
- Программная навигация через `useRouter`
- Breadcrumb навигация на страницах

### Оптимизация

**Изображения:**
- Использование `next/image` для оптимизации
- Lazy loading изображений
- Responsive images

**Производительность:**
- Server-side rendering для статических страниц
- Client-side rendering для динамических данных
- Suspense boundaries для асинхронных компонентов
- Кэширование данных через React hooks

---

## 🔧 Команды разработки

```bash
# Запуск dev сервера
npm run dev

# Запуск с очисткой кэша
npm run dev:clean

# Запуск на альтернативном порту
npm run dev:alt

# Остановка процесса на порту 3000 и запуск
npm run dev:kill

# Сборка для production
npm run build

# Запуск production сервера
npm start

# Линтинг
npm run lint

# Работа с базой данных
npm run db:migrate    # Применить миграции
npm run db:generate   # Сгенерировать Prisma Client
npm run db:seed       # Заполнить базу данных
npm run db:studio     # Открыть Prisma Studio
```

---

## 📦 Зависимости

### Production
- `next` - Next.js framework
- `react` - React library
- `react-dom` - React DOM
- `@prisma/client` - Prisma Client
- `@prisma/adapter-better-sqlite3` - SQLite адаптер для Prisma
- `better-sqlite3` - SQLite драйвер
- `lucide-react` - Иконки
- `dotenv` - Переменные окружения

### Development
- `typescript` - TypeScript
- `@types/node` - Типы для Node.js
- `@types/react` - Типы для React
- `@types/react-dom` - Типы для React DOM
- `tsx` - TypeScript executor
- `prisma` - Prisma CLI
- `tailwindcss` - Tailwind CSS
- `autoprefixer` - Autoprefixer
- `postcss` - PostCSS

---

## 🗂️ Структура проекта

```
Losawa/
├── app/                    # Next.js App Router
│   ├── api/               # API routes
│   │   ├── products/      # Products API
│   │   ├── collections/   # Collections API
│   │   ├── categories/    # Categories API
│   │   ├── orders/        # Orders API
│   │   ├── debug/         # Debug endpoint
│   │   └── health/        # Health check
│   ├── category/          # Страницы категорий
│   ├── product/           # Страницы товаров
│   ├── shop/              # Страница магазина
│   ├── about/             # Страница "About Us"
│   ├── layout.tsx         # Root layout
│   ├── page.tsx           # Главная страница
│   └── globals.css        # Глобальные стили
├── components/            # React компоненты
│   ├── Header.tsx
│   ├── Footer.tsx
│   ├── Hero.tsx
│   ├── Collections.tsx
│   ├── ShopByCategoryFilter.tsx
│   ├── BestsellersBlock.tsx
│   └── ...
├── hooks/                 # React hooks
│   ├── useProducts.ts
│   └── useCollections.ts
├── lib/                   # Утилиты
│   ├── prisma.ts         # Prisma Client
│   └── api.ts            # API функции
├── data/                  # Статические данные
│   ├── products.ts
│   └── collections.ts
├── prisma/                # Prisma схема и миграции
│   ├── schema.prisma
│   ├── migrations/
│   └── dev.db            # SQLite база данных
├── scripts/               # Скрипты
│   └── seed-db.ts        # Seed скрипт
├── public/               # Статические файлы
│   ├── products/         # Изображения товаров
│   ├── logo-header.png
│   └── logo-footer.png
└── package.json
```

---

## 🚀 Развертывание

### Локальная разработка
1. Установить зависимости: `npm install`
2. Настроить базу данных: `npm run db:migrate`
3. Заполнить данными: `npm run db:seed`
4. Запустить сервер: `npm run dev`

### Production
1. Собрать проект: `npm run build`
2. Запустить сервер: `npm start`

### Переменные окружения
- `DATABASE_URL` - Путь к SQLite базе данных (по умолчанию: `file:./prisma/dev.db`)
- `NODE_ENV` - Окружение (development/production)

---

## 📝 Примечания

- Все товары и коллекции загружаются из базы данных через API
- Корзина хранится в локальном состоянии (не сохраняется между сессиями)
- Фильтрация и сортировка выполняются на клиенте после загрузки данных
- Изображения товаров должны находиться в `/public/products/`
- Логотипы должны находиться в `/public/`

---

## 🔄 Будущие улучшения

- [ ] Сохранение корзины в localStorage
- [ ] Страница корзины с возможностью оформления заказа
- [ ] Страница оформления заказа
- [ ] Аутентификация пользователей
- [ ] Личный кабинет пользователя
- [ ] История заказов
- [ ] Админ-панель для управления товарами
- [ ] Поиск по товарам
- [ ] Пагинация товаров
- [ ] Избранные товары
- [ ] Отзывы и рейтинги товаров
- [ ] Интеграция с платежными системами
